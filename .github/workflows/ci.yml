# CI mirrors local work: bootstrap installs, check validates. If it passes
# locally it passes here, and vice versa. The one divergence: buf and sqlc
# run as prebuilt binaries (same versions as go.mod) instead of `go tool`,
# because compiling them from source costs ~2 minutes on a cold Go cache.
name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version-file: go.mod

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.14

      # Versions must match the tool versions pinned in go.mod
      # (github.com/bufbuild/buf, github.com/sqlc-dev/sqlc).
      - uses: bufbuild/buf-action@91da6f6a1a2c877c182debad24ca0a8a9d848be7 # v1
        with:
          version: 1.71.0
          setup_only: true

      - uses: sqlc-dev/setup-sqlc@bac53b7fb28c039a6c7f5736fd1e89744021bdd6 # v5
        with:
          sqlc-version: "1.31.1"

      - run: make bootstrap
      - run: make check BUF=buf SQLC=sqlc

      # docs/routes.json is the one tracked, CI-gated artifact from `make
      # docs` (see docs/plan-v6.md Issue 5): regenerate it and fail if the
      # working tree's committed copy doesn't match, so a route/state added
      # or changed without updating docs/routes.json is caught in review.
      # Chromium is already installed by `make bootstrap` above.
      - run: make docs
      - run: git diff --exit-code docs/routes.json
